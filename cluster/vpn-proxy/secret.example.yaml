# TEMPLATE ONLY — do NOT put the real key here or commit it.
# The real Secret is created out-of-band with kubectl (see README.md),
# matching how other secrets in this repo are handled (not stored in git).
apiVersion: v1
kind: Secret
metadata:
  name: proton-wg
  namespace: vpn-proxy
type: Opaque
stringData:
  WIREGUARD_PRIVATE_KEY: "REPLACE_WITH_PRIVATE_KEY_FROM_PROTON_CONF"
