apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: browserless
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/AimAmit/infra.git'
    path: cluster/browserless
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: browserless
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
