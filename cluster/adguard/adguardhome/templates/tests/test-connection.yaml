apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adguardhome.fullname" . }}-test-connection"
  labels:
    {{- include "adguardhome.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adguardhome.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
