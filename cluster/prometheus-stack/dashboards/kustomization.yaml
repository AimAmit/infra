apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - dashboard-cluster-monitoring.yaml
  - dashboard-node-exporter.yaml
  - dashboard-nginx-ingress.yaml
  - dashboard-k8s-views-pods.yaml
  - dashboard-k8s-views-global.yaml

commonLabels:
  grafana_dashboard: "1"

commonAnnotations:
  grafana_folder: "Kubernetes System"
