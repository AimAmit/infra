apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: codex-lb
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/AimAmit/infra.git'
    path: cluster/codex-lb
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: codex-lb
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
