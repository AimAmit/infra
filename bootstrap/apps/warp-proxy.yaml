apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: warp-proxy
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/AimAmit/infra.git
    targetRevision: HEAD
    path: projects/warp-proxy
  destination:
    server: https://kubernetes.default.svc
    namespace: warp-proxy
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
