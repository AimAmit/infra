apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vpnbook-proxy
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/AimAmit/infra.git
    targetRevision: HEAD
    path: cluster/vpnbook-proxy
  destination:
    server: https://kubernetes.default.svc
    namespace: vpnbook-proxy
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
